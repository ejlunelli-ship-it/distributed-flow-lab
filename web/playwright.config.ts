import { defineConfig, devices } from '@playwright/test'

/**
 * E2E configuration. Playwright boots the Vite dev server and drives the full
 * canvas → simulation → animation flow (.docs/05-dev/testing.md). For scaffolding
 * it runs a single smoke spec against the app shell.
 */
export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
  },
  projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
})
